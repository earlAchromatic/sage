interface:
  display_name: "SAGE"
  short_description: "Review PRs in Jacob's Reviewable style"
  default_prompt: "Use $sage to perform a deep Reviewable-oriented code review."
dependencies:
  tools:
    - type: "mcp"
      value: "sage-reviewer"
      description: "Reviewable MCP server for review discussions, dispositions, and PR context"
