interface:
  display_name: "SAGE"
  short_description: "Review and verify PRs in Jacob's style"
  default_prompt: "Use $sage to perform a deep Reviewable-oriented code review, verify the SAGE identity and live review protocol, and test affected browser workflows where appropriate."
dependencies:
  tools:
    - type: "mcp"
      value: "sage-review"
      description: "SAGE Reviewable MCP server for review state, discussions, dispositions, file marks, and publication"
