interface:
  display_name: "SAGE"
  short_description: "Review, verify, and improve PR reviews"
  default_prompt: "Use $sage to perform and verify a deep Reviewable-oriented code review, then debrief it and open a separate SAGE improvement PR for any qualified process gap."
dependencies:
  tools:
    - type: "mcp"
      value: "sage-review"
      description: "SAGE Reviewable MCP server for review state, discussions, dispositions, file marks, and publication"
